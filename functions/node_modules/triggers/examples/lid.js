var monitor = require('./..'),
    opts = { interface: 'org.freedesktop.Hal.Device' };

monitor.watch(opts, function(err, lid) {

  lid.on('event', function(name, args) {
    console.log(name, args);
  });

})

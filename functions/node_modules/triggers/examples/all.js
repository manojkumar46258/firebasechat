var triggers = require('./..');

triggers.on('event', function(name, args) {
  console.log('New event : ' + name, args);
});

triggers.on('killed', function() {
  console.log('Killed!');
})

triggers.watch('all', { respawn: true });
